import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lapinragnar.angular',
  appName: 'nx-monorg',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
